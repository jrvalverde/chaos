/************************************************	Mandzoom4.c**  Generates a RAW format file of an image *  of Mandelbrot set (grayscale)**  C source file by Alberto Strumia*************************************************/#include<stdio.h>/* definition of constants */#define Radius 10#define Cx 0.745400#define Cy 0.11312#define Side 0.01#define M 700#define Num 512/* Alternative values Side 0.001, 0.0001 Num 1024 , 2048 *//* main program */main(){	int p, q, n, w;	double x, y, xx, yy, Incx, Incy;		FILE *fp;	fp = fopen("Mand4.pgm","w");    	fprintf(fp, "P2\n%d %d\n256\n", M, M, Num);			for (p = 1; p <= M; p++)	{		Incy = Cy - Side + 2*Side/M*p;					printf("%i %%\n", p*100/M);				for (q = 1; q <= M; q++)		{			Incx = Cx - Side + 2*Side/M*q;					x = 0;		y = 0;		w = 200;					for ( n = 1; n <= Num; ++n)			{				xx = x*x - y*y - Incx;				yy = 2*x*y - Incy;								x = xx;				y = yy;								if ( x*x + y*y > Radius )					{						w = n;						n = Num;					}			}		fprintf(fp, "%d\n", w/2 );		}	}		fclose(fp);}/* end of main program */